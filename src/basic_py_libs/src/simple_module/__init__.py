# Setup file, the root of imports
"""
    A basic module used for demonstrating Python modules
"""

def say_hello(name):
    """
        Say hello to a `name`
        Parameters:
        - name: str
            Name to say hello to
    """
    print("Hello, {}".format(name))
